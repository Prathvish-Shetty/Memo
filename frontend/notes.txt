server side retrieval vs client side local storage to persist state after refresh
i used server side retrieval in lifeline project and will use localstorage in this one

The replace prop in <Navigate to="/login" replace /> ensures that when a user is redirected, the previous page (protected route) is removed from the browser's history.